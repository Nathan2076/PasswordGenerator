name: g++ build

# Runs on general and C++-specific branches only, including in pull requests
on:
  push:
    branches:
      - main
      - 'release/**'
      - 'cpp/**'

jobs:
  linux:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Check g++ version
      run: g++-12 --version
    - name: Build
      run: g++-12 ./C++/main.cpp ./C++/chars.h
  windows:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v3
    - name: Check g++ version
      run: C:\msys64\ucrt64\bin\g++.exe --version
    - name: Build
      run: C:\msys64\ucrt64\bin\g++.exe ./C++/main.cpp ./C++/chars.h
